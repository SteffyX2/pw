#include <stdio.h>
#include <stdlib.h>

int main()
{
    int z,i;
    printf("Unesite duzinu niza (z<=50):");
    do{
    scanf("%d", &z);
    }while(z>50);
    int niz[z];;
    for(i=0;i<z;i++)
    {
        printf("\nUnesite %d. broj:", i+1);
        scanf("%d", &niz[i]);
    }
    for(i=0;i<z;i++)
    {
        niz[i]+=5;
    }
        for(i=0;i<z;i++)
    {
        printf("\n %d. clan niza je: %d",i+1,niz[i]);
    }
}

