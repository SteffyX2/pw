#include <stdio.h>
#include <stdlib.h>

int prost(int n)
{
    int i,x=n,j=0;
    for(i=0;i<n;i++)
    {
        if(x!=n)
        if(x!=1)
        if((n%x)==0)
            j++;
        x--;
    }
    return j;
}

int main()
{
    int n=0,x,i;
    printf("Unesite x:");
    scanf("%d", &x);
    for(i=n;i<=x;i++)
    {
    if(prost(n)==0)
        printf("\n%d", n);
    n++;
    }
}
